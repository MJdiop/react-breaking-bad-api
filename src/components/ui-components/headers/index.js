import logo from './../../../img/logo.png';

const Header = () => {
  return (
    <header className="center">
      <img src={logo} alt="Breaking Bad API" />
    </header>
  );
};

export default Header;
